'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Declare name variable and initialised it with undefined value
let name = undefined;

// Log name variable with undefined value
console.log(name);