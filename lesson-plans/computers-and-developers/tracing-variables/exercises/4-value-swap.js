'use strict';

/* Value Swap

  Can you predict what the [trace] button will log to the console?
    use the [table] button to write down your predictions

  study tips: [variables], [open in] jsTutor, [ask me]

*/
// declare variable a and initialise with value "y" //
let a = 'y';

// declare variable b and initialise with with value "x" //
let b = 'x';

// declare variable t and initialise with with value " " //
let t = '';

// read variables a, b, t with values y, x, " "//
console.log(a, b, t);

// read variable a with value y //
// assign variable t value of variable a which is "y" //
t = a;

// read variable b with value x //
// assign variable a value of variable b which is "x" //
a = b;

// read variable t with value y //
// assign variable b value of variable t which is "y" //
b = t;

// read variables a, b, t with values x, y, y//
console.log(a, b, t);


// what did you do in this program?
// what are the final values of `a` and `b`?
// how are they changed from the initial values

// is it possible to swap a and b without using t?
